"""
Command implementations for the infomux CLI.

Each submodule implements a CLI subcommand with:
- configure_parser(parser): Add arguments to the subcommand parser
- execute(args): Run the command and return an exit code
"""
